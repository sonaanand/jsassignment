input=prompt("enter the number:");
while(input){
    var check=input==null ? true:false;
    {
        loop=(input>100||check) ? userprompt =false:input=Number(prompt("enter the number:"));
    }
   
}
